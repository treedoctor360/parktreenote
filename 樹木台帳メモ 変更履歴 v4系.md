# 樹木台帳メモ v4系 変更履歴・設計記録

記録日: 2026-07-21
対象: 樹木台帳メモ v3.2 → v4.5
作成: Koh Kitsukawa（大津市公園緑地課）／実装補助: Claude

---

## 0. この改修の目的

樹木台帳アプリのデータを、**プリザンター（庁内低コード基盤）の正式台帳に引き継ぐ**こと。
アプリのデータは各端末のIndexedDBに閉じており、9月末の転出後に組織へ残らない。
プリザンターに正式台帳を置くことで、課内共有・更新履歴・検索集計・引き継ぎ資産化を実現する。

**役割分担（結論）**

| | 担当 |
|---|---|
| 現場のメモ・高速入力・見積転記 | 樹木台帳アプリ（従来通り） |
| 正式台帳・共有・引き継ぎ | プリザンター |

橋渡しは**CSVの手動インポート**。API連携は環境制約（APIキー取得不可・外部接続不可）により採用しない。

---

## 1. バージョン別の変更内容

### v4.0 — プリザンター用CSV出力の新設

- ヘッダーに「プリザンター」ボタンを追加。公園タブの絞り込みに従って出力
- 出力モーダルで「未出力のみ（既定）」「すべて」を件数付きで選択
- CSV 12列: タイトル／公園名／樹種／種別／剪定種別／幹周／幹周ランク／単位／数量／調査日／メモ／台帳ID
  - 幹周: 単一実数のみ数値化（c120→120）。範囲表記（c60-90）は空欄
  - 幹周ランク: 集計パネルと同じロジックで再計算（C=60-90 等）
- 出力済み管理: 出力したレコードに「P出力済」バッジ。編集・剪定種別変更で未出力に戻る
- **所在地・開設日・学区・学区内番号は出力しない**（公園マスタのLookupsで自動入力されるため）
- 既存のCSV・JSON・転記テキスト出力は無変更

### v4.1 — 台帳IDの固有化・公園マスタ照合

**台帳ID（uid）の新設**
- 従来の台帳IDはIndexedDBの内部連番で、JSON復元のたびに変化していた（重複の温床）
- 作成時に一度だけ振る固有ID（例: TMROJQS5P-N517）に変更。編集・復元・端末間移動でも不変
- 既存レコードには初回起動時に自動付与

**JSON復元の2モード化**
- 従来は「IDを捨てて全件新規追加」だったため、復元のたびに全件重複していた
- 置換復元（推奨）: 現データを消して復元。台帳ID・内部IDとも維持
- 統合: 同じ台帳IDは上書き、新しいものだけ追加。別端末のデータ合流用

**公園マスタ（プリザンター表記合わせ）**
- マスタタブに公園マスタCSV読込を追加（UTF-8/Shift_JIS自動判定、複数ファイル統合可）
- 公園名の列は「児童遊園地名 / 公園名 / タイトル」のいずれかを自動検出
- 入力候補（datalist）がマスタ表記になる
- 保存時に照合し、表記ゆれは自動修正（穴太1丁目→穴太一丁目、霞ヶ丘→霞ケ丘、全角数字・空白等）
- マスタ不一致は候補付き警告（保存は可能）
- プリザンター出力モーダルにも不一致警告を表示

### v4.2 — 公園名の一括編集

- 旧UI（ドロップダウン2つで1組ずつ置換）を廃止。公園数が多く探しにくかったため
- 「公園名を一括編集」モーダル: 台帳内の全公園名を一覧表示（件数・マスタ一致状況付き）
- 表記ゆれの行にはマスタ表記が**あらかじめ入力済み**。確認して適用するだけ
- 修正先入力欄はdatalist方式で、文字入力により候補が絞り込まれる
- 複数の公園名をまとめて一度に適用可能。変更した行は未出力に戻る

### v4.3 — 計上区分の新設

**背景（設計上の重要な発見）**
「種別」列が2つの意味を兼務していた。
- 樹木としての分類（変わらない事実）: サザンカ=常緑広葉樹、アジサイ=落葉低木
- 見積上の計上区分（場面で変わる判断）: 低木寄植え(m2)／低木生垣(m)／低木玉もの(株)／単木で高木扱い(本)

同じシラカシでも単木なら常緑広葉樹・本、生垣なら低木生垣・m。
これが「種別が分かりにくい」の正体だった。

**採用した設計**
- **種別（樹種分類）は現状維持**。落葉広葉樹／常緑広葉樹／針葉樹／低木の4分類。プリザンターの「種別」列に入れる
- **計上区分を新列として追加のみ**。既存の集計・転記の仕組みには一切触らない

理由: **転記テキスト（見積査定システムへの受け渡し）が「種別=低木(L)」を鍵に低木寄植え(m2)へ振り分けている**ため、種別から低木を外すと見積転記が壊れる。既存出力を壊さない原則を優先した。
振り分けの鍵を計上区分ベースに作り替えるのは影響が大きいため、**別段階の課題として保留**。

**計上区分の選択肢**（見積査定システムの単価マスタ「種類」に準拠）

| 区分 | 単位 |
|---|---|
| 高木剪定 | 本 |
| マツ仕立て | 本 |
| 樹木伐採 | 本 |
| 低木(機)寄植え | m2 |
| 低木(手刈)寄植え | m2 |
| 刈込(生垣)機械 | m |
| 生垣手刈り | m |
| 低木玉物(手刈) | 株 |
| フジ棚(冬季) | m2 |

- 剪定種別（強/軽/伐採）は別列のため、区分名にカッコ書きの剪定強度は含めない
- 編集画面で計上区分を選ぶと単位が自動セットされる
- 単位セレクトに「m」（生垣用）を追加
- プリザンターCSVは既存12列を保ったまま**末尾に「計上区分」列を追加**（13列目）

### v4.4 — 樹種マスタ追加・データ誤記修正

- 樹種マスタに **タイワンフウ = 落葉広葉樹** を追加（街路樹で今後も頻出のため）
- 既存データの誤記を修正（3件）: イボタノノキ→イボタノキ、カイズカイブキ→カイヅカイブキ
- 修正後に種別を再判定した結果、**全246件で種別の空欄が解消**

### v4.5 — 株立の幹周自動計算

**記法**（テキスト一括入力・フォーム・編集のどこでも使える）

```
キョウチクトウ　c100+50+80        → 幹周 c161
キョウチクトウ　c100+c50+c80      → 同上（cを毎回書いてもよい）
```

全角＋・全角ｃ・区切りの空白も許容する。

**計算**: (各幹の幹周の合計) × 0.7、小数は**切り捨て**
例: (100+50+80) × 0.7 = 161 → c161

**幹の本数による条件分けはしない**（2本立でも同じ0.7を掛ける）。

**メモへの自動追記**: 内訳を `株立 c100+c50+c80 → c161` の形で自動でメモに残す。
プリザンターCSVのメモ列にも出るため、取込後も換算の根拠を追える。

**実装上のポイント**
- 計算は共通関数 `parseKabudachi()` に集約。テキスト一括入力（parseLine）と
  フォーム・編集（normalizeRecord）の両方から呼ぶ
- テキスト一括入力では、各幹の値が「2番目以降のc値」や残余メモに混ざらないよう、
  株立の記述を行から取り除いてから他の項目を解析する
- 換算後の幹周は単一値（c161）になるため、幹周ランク・集計・転記テキスト・
  プリザンターCSVの数値化はすべて既存ロジックのまま動く
- **二重計算しない**: 保存後の幹周は c161 で「+」を含まないため、
  再編集してもメモに内訳が重複追記されない
- **既存データに影響しない**: 「+」を含まない従来の記法（c150、c60-90 等）は素通り。
  既存の `//株立` メモがあるレコードも遡って再計算しない

**浮動小数点誤差への対策（重要）**
JavaScriptでは `90 × 0.7 = 62.99999999999999` となり、単純な切り捨てでは
本来63cmのものが62cmになる。全数検証で10刻みの合計値のうち70パターンで
1cmのずれが発生することを確認したため、丸めてから切り捨てる実装にしている。
修正後は1〜5000の全整数で誤差ゼロを確認済み。

---

## 2. 既存データの移行結果（246件・0721バックアップ）

変換済みJSON: `樹木台帳バックアップ_v4-4変換済_0721.json`
取込方法: v4.4を開く → JSON復元 → **置換復元**

**種別（樹種分類）**

| 種別 | 件数 |
|---|---|
| 低木 | 97 |
| 落葉広葉樹 | 74 |
| 常緑広葉樹 | 61 |
| 針葉樹 | 14 |
| 空欄 | 0 |

**計上区分（初期値は【推定】。後から編集可）**

| 計上区分 | 件数 |
|---|---|
| 高木剪定 | 132 |
| 低木(機)寄植え | 86 |
| 樹木伐採 | 17 |
| 低木玉物(手刈) | 2 |
| 空欄（要手動確認） | 9 |

**推定ルール**
- unit=m2 → 低木(機)寄植え（機械を初期値。手刈は後で変更）
- unit=株 → 低木玉物(手刈)
- unit=m → 刈込(生垣)機械
- unit=本＋剪定種別=伐採 → 樹木伐採
- unit=本＋種別が高木（落葉/常緑/針葉） → 高木剪定
- unit=本＋種別=低木 → **空欄**（単木か寄植えか機械判断できないため）

空欄9件の樹種: ヒラド・アジサイ・ナンテン・ユキヤナギ・サザンカ・シモツケ

---

## 3. 確定したプリザンターの仕様（実機確認・2026年7月）

- インポートは**CSVヘッダー名と項目の表示名の一致**で取り込む。列の並び順は無関係
- 表示名が1文字でも違うとその列は取り込まれない
- リンク設定した分類項目（公園名）は**公園名の文字列のままインポート可能**。一致すればLookupsも動き、所在地・開設日・学区・学区内番号が自動入力される
- **添付ファイルはCSVインポートで取り込めない**（写真の一括移行は不可）
- インポートでの既存レコード更新は「**ID列（プリザンター内部ID）の一致**」のみ。任意キー（台帳ID等）での更新は不可
- エクスポート設定は「エディタ」でよい

**エクスポート/インポートの制約から来る運用**
アプリのCSVにはプリザンター内部IDが無いため、同じデータを再取込すると重複行になる。
→ 修正分を取り込む前に、プリザンター側で**台帳IDを検索して旧行を削除**する（台帳IDは不変なので確実に探せる）

---

## 4. 採用しなかった案と理由

| 案 | 却下理由 |
|---|---|
| API連携（$p.set / apiUpdate / 添付ファイルAPI） | APIキー取得不可・外部接続不可（セキュリティ上）。実現手段がない |
| 拡張HTMLへのv10系コード埋め込み | CSS・印刷（@media print）がプリザンター画面を破壊。JS衝突リスク。36万字を設定欄に貼る運用は保守不能 |
| クリップボード連携（転記テキスト方式） | 実用性が低いと判断され取り止め |
| 表形式入力タブの新設 | 現場フローが「iPhoneメモ帳→ペースト」のため、アプリ内の表入力はフローに乗らない |
| テキスト解析の寛容化 | 業者名メモ（例: 浅井、創旬堂）や作業断片を誤って樹種として拾う危険。逆に**樹種マスタ照合による厳密化**が正しい方向 |
| 公園マスタ対応表による所在地等の展開 | 公園名一致でLookupsが動くと実機確認できたため不要になった |
| 公園名のID変換 | 公園名の文字列のまま取込可能と確認できたため不要になった |

---

## 5. 保留・今後の課題

1. **プリザンター往復連携（v4.2案として提案・保留中）**
   プリザンターの樹木テーブルをエクスポートしてアプリに読み込ませ、各レコードにプリザンター内部IDを紐付ける。以後は「ID」列付きCSVで出力すれば、インポート時に上書き更新され重複しない。同じ仕組みで、プリザンター側で直接入力されたレコードをアプリに取り込むことも可能になる。

2. **転記テキストの振り分けを計上区分ベースへ**
   現在は「種別=低木(L)」が低木寄植え(m2)への振り分けの鍵。計上区分ベースに変えれば見積査定システム側の手修正が減るが、転記ロジックの変更は影響が大きく、見積査定システムとの境界設計に踏み込むため保留。

3. **テキスト一括入力の樹種マスタ照合**
   「マスタにある樹種で始まる行だけ拾う」方式への変更。未認識行は登録しつつ要確認マークを付ける案まで設計済み。樹木知識のない後任者でも種別が自動で埋まるようになる。種別の二重性（課題2）の整理が前提。

4. **表形式入力タブ**
   後任者（新規大量入力・樹木知識が浅い）向け。まず樹種マスタ照合でメモ方式のハードルを下げ、それでも足りなければ追加する順序。

5. **写真を除いたバックアップ出力**
   写真が多いとJSONが肥大し、プリザンターの添付上限に掛かる可能性がある。必要になれば追加。

---

## 6. バックアップの共有運用（複数人利用）

プリザンターに専用の記録テーブル「樹木台帳バックアップ」を作り、**1人1レコード**で運用する。

- 項目: タイトル（例「橘川 樹木台帳」）／添付ファイルA（最新JSON）／説明（件数・日付メモ）
- 自分のバックアップを置く: アプリのJSON出力 → 自分のレコードの添付を差し替えて更新（常に最新1つだけ）
- 他人のデータを取り込む: 相手のレコードからJSONをダウンロード → アプリの復元で「**統合**」を選ぶ（同じ台帳IDは上書き・新規のみ追加なので重複しない）

**注意**
- 同じレコードを2人が別々に編集していた場合、統合では後から取り込んだ側で上書きされる。公園単位で担当を分けるのが安全
- v4.0以前のJSON（台帳IDなし）は統合に使えない。v4.1以降で出力し直すこと

---

## 7. 開発上の注意（次にこのアプリを触る人へ）

**守るべきルール**

- 単一HTML構成。`'use strict'` のため変数は必ず `var` 宣言。ES5構文で書く
- コード変更後は必ず構文チェック（HTMLからscript部を抽出して `node --check`）
- **既存のCSV・JSON・転記テキストの出力形式は変更しない**。追加は末尾列など後方互換の形で行う
- 出力内容が変わる変更をしたら `pExportedAt` を null に戻す（再出力の対象にするため）
- 版番号はコード変更のたびに更新（title・フッターの2箇所）
- **転記テキストは「種別=低木(L)」を鍵に低木寄植え(m2)へ振り分けている**。種別の分類をいじると見積転記が壊れる

**今回の検証記録**

- 全バージョンで `node --check` による構文確認を実施（HTMLからscriptを抽出して検証）
- 計上区分の移行は、実バックアップ246件に対しアプリと同一のロジックでシミュレーションしてから適用
- 公園名照合ロジックは、表記ゆれ6パターン（全角数字・全角空白・ヶ/ケ・漢数字・完全一致・不一致）で検証済み
- 株立計算は、記法6種・端数・二重計算防止・既存データ非影響を検証。浮動小数点誤差は1〜5000の全整数で確認
- 変換済みJSONは元ファイルを変更せず別名で出力
- 既存の CSV・JSON・転記テキストの出力形式は v4.0〜v4.5 を通じて**一切変更していない**

---

© 2026 Koh Kitsukawa. All rights reserved.
