# 樹木台帳メモ v4.5 → プリザンター取込手順書

作成: 2026-07-21（v4.5版に更新）

## 1. プリザンター側の事前設定（最初に1回だけ）

樹木テーブルの「管理」→「テーブルの管理」→「エディタ」タブで、以下の項目を有効化し、表示名を設定する。

| CSV列名（=表示名） | 項目 | 備考 |
|---|---|---|
| タイトル | タイトル | 中身は「樹種 数量単位 公園名」（例: ケヤキ 2本 穴太一丁目） |
| 公園名 | 分類B | 設定済（公園マスタへのリンク） |
| 樹種 | 分類（空き） | |
| 種別 | 分類（空き） | 落葉広葉樹/常緑広葉樹/針葉樹/低木 |
| 剪定種別 | 分類（空き） | 強剪定/軽剪定/伐採 |
| 幹周 | **数値**（空き） | 単一実測値のみ入る（単位: cm） |
| 幹周ランク | 分類（空き） | C=60-90 等の文字列 |
| 単位 | 分類（空き） | 本/m2/株（数量の単位） |
| 数量 | 数値（空き） | |
| 調査日 | 日付（空き） | 日付Aは開設日に使用中のため別の日付項目に |
| メモ | 説明（空き） | |
| 台帳ID | 説明（空き） | アプリが作成時に振る固有ID（例: TMRO…-N517）。重複確認・照合用 |
| 計上区分 | 分類（空き） | 発注用の区分（単価マスタの種類名に準拠）。v4.3新設・任意 |

**重要**: 表示名が1文字でも違うとその列は取り込まれない。
所在地・開設日・学区・学区内番号はCSVに含めない。公園名が公園マスタと一致すれば自動入力される（動作確認済み）。

## 2. アプリ側の初回設定: 公園マスタの読込（v4.1新機能）

1. プリザンターの公園一覧（児童遊園地一覧）を一覧画面からCSVエクスポートする
   - 一覧に「ID」と「児童遊園地名」の列が表示されていること（学区等は不要）
2. アプリの「🌿 マスタ」タブ →「🏞 公園マスタ」→「📤 CSV読込」でそのCSVを読み込む
3. 「n件登録済み」と表示されれば完了。効果:
   - 公園名の入力候補がプリザンターの正式表記になる（頭2〜3文字で候補から選べる）
   - 表記ゆれは保存時に自動修正される（穴太1丁目→穴太一丁目、霞ヶ丘→霞ケ丘、全角数字・空白など）
   - マスタに無い公園名は保存時に警告が出る（保存自体は可能）
   - プリザンター出力時にも不一致レコードが警告表示される
4. 都市公園など別の一覧を追加で読み込むと統合される（公園名の列: 児童遊園地名/公園名/タイトル のいずれかがあれば読める。文字コードはUTF-8/Shift_JIS自動判定）

**既存データの表記修正（v4.2）**: 「📝 公園名を一括編集」で台帳内の全公園名を一覧表示（件数・マスタ一致状況付き）。表記ゆれの行にはマスタ表記が自動で入っているので、確認して「変更を適用」を押すだけ。その他の行も、入力欄をタップ→文字を入力するとマスタ候補が絞り込まれるため、公園数が多くても探しやすい。空欄の行は変更されない。

## 3. 日常の運用手順

1. 現場で台帳アプリに入力（従来通り）。公園名は候補から選ぶ
2. ヘッダー「**プリザンター**」ボタン →「**未出力のみ出力（n件）**」→ CSVダウンロード
   - 公園タブで特定の公園を選択中なら、その公園だけが出力対象
3. プリザンターの樹木テーブル一覧 →「インポート」→ 文字コード **UTF-8** で取込
4. 取込後、所在地・学区などの自動入力を数件確認

## 4. 重複防止と修正時の運用

- 出力済みレコードには「P出力済」バッジが付き、「未出力のみ出力」に含まれない
- アプリで**編集・剪定種別変更・公園名一括修正**をすると未出力に戻り、再出力対象になる
- **修正分を取り込むときの注意**: 再取込すると新しい行として追加される。取込む前に、プリザンター側で**台帳IDで検索して旧い行を削除**すること（台帳IDは編集しても変わらないので確実に探せる）
- 「すべて出力」は初回移行・作り直し用。使う前にプリザンター側の該当レコードを削除する

## 5. 台帳IDとバックアップ復元（v4.1で仕様変更）

- **台帳ID**: 作成時に一度だけ振られる固有ID。編集・バックアップ復元・端末間の受け渡しでも**変わらない**（v4.0以前はアプリ内部の連番で、復元のたびに変わっていた）
- **JSON復元**は2方式から選ぶ:
  - **置換復元（推奨）**: 今のデータを消してバックアップの内容に置き換える。機種変更・復元用。台帳IDも維持される
  - **統合**: 同じ台帳IDのレコードは上書き、新しいものだけ追加。別端末のデータを合流させる用途
  - v4.0以前のバックアップ（台帳IDなし）は「置換復元」を使う

## 6. 動作確認テストケース

1. 公園マスタCSV読込 → 件数表示、公園名入力欄の候補がマスタ表記になる
2. 「穴太1丁目」で保存 → 「穴太一丁目」に自動修正されるトーストが出る
3. マスタに無い公園名で保存 → 警告ダイアログ（候補付き）が出る
4. プリザンター出力 → 未出力/すべての件数、マスタ不一致警告の表示
5. 出力後にP出力済バッジ → 編集するとバッジが消える
6. CSVの列名・タイトル（樹種 数量単位 公園名）・台帳ID（T…形式）を確認
7. JSONバックアップ → 置換復元 → 台帳IDが変わっていないこと、重複しないことを確認
8. プリザンターに取込 → 公園マスタからの自動入力を確認

## 7. 今回確定したプリザンターの仕様（引き継ぎメモ）

- インポートはCSVヘッダー名と項目の表示名の一致で取り込む（並び順は無関係）
- リンク設定した分類項目（公園名）は公園名の文字列で取込可能、一致すればLookupsも動く（2026-07実機確認）
- 添付ファイルはCSVインポートでは取り込めない（写真の一括移行は不可）
- インポートでの既存レコード更新は「ID列（プリザンター内部ID）一致」のみ。任意キーでの更新は不可

## 8. 計上区分（v4.3・種別との違い）

「種別」と「計上区分」は役割が違う。

- **種別（樹種分類）**: 木の性質。落葉広葉樹／常緑広葉樹／針葉樹／低木。樹種から自動で決まる（知識不要）。プリザンターの「種別」列に入る
- **計上区分**: 発注のための区分。見積査定システムの単価マスタ「種類」に準拠。人が選ぶ任意項目。プリザンターの「計上区分」列に入る

計上区分の選択肢（単位）: 高木剪定(本)／マツ仕立て(本)／樹木伐採(本)／低木(機)寄植え(m2)／低木(手刈)寄植え(m2)／刈込(生垣)機械(m)／生垣手刈り(m)／低木玉物(手刈)(株)／フジ棚(冬季)(m2)。編集画面で計上区分を選ぶと単位が自動で合う。

**既存レコードの初期値（推定）**: v4.3を最初に開いたとき、既存データに計上区分の初期値が自動で入る。内訳の目安は、面積もの(m2)→低木(機)寄植え、株→低木玉物(手刈)、伐採→樹木伐採、高木の本→高木剪定、低木の単木(本)→空欄（手作業で確認）。手刈/機械の別など、推定では決められないものは編集画面で直す。

---

## 9. バックアップの共有運用（複数人利用・推奨）

プリザンターに専用テーブル「**樹木台帳バックアップ**」（記録テーブル）を1つ作り、**1人1レコード**で運用する。

- 項目: タイトル（例:「橘川 樹木台帳」）、添付ファイルA（JSONを格納）、説明（件数・日付などのメモ）
- **自分のバックアップを置く**: アプリの「JSON」出力 → 自分のレコードを開き、添付ファイルAの古いJSONを削除して新しいJSONを添付 → 更新。常に「最新が1つだけ」の状態を保つ
- **他の人のデータを取り込む**: その人のレコードからJSONをダウンロード → アプリの「復元」で読み込み →「**統合**」を選ぶ（同じ台帳IDは上書き・新しいものだけ追加されるので重複しない）
- **注意**:
  - 同じレコードを2人が別々に編集していた場合、統合では後から取り込んだ側の内容で上書きされる。公園単位で担当を分ける運用を推奨
  - v4.0以前のJSON（台帳IDなし）は統合に使えない。v4.1以降で出力し直すこと
  - 写真を多く登録しているとJSONが大きくなり、プリザンターの添付上限（管理者設定）を超えて添付できないことがある。その場合は相談（写真を除いたバックアップ出力の追加を検討）
