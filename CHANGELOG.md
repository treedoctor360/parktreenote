# 変更履歴 — 樹木台帳メモ

## v3.2（2026-07-15）

現場メモの解析・集計を見積査定ツール v10-59 への転記に対応させる改修。
（v3.2 改修仕様書のタスク A〜H ＋ 摘要記法の追加。v10-59 側の修正は不要）

### 解析（テキスト一括入力）

- **w×d からの面積自動計算**: 行に `w数値` と `d数値` が両方あれば面積(m2)を自動計算
  （単位は cm、`w500 d200` → 10.00m2）。`×N個` `×N箇所` があれば N 倍。
  `=Xm2` の明示があればそちらを最優先。`N本` 併記時は面積優先で本数はメモへ
- **樹高の扱いを拡張**: `h150以下` 等のランク付き h は規格へ（従来どおり）、
  裸 h（`h120` 等）は「実測」としてメモに保持。裸 h のみの行は裸 h を規格に使う
- **ゴミ行の自動スキップ**: `---` 行で公園をリセットし、次の `#` までの品目行を無視。
  これによりファイル先頭の日付行・各ブロック先頭の班名行が自動で除外される。
  行頭 `//` または `※` はコメント行としてスキップ
- **インライン摘要 `//`**: 行中の `//` 以降は解析せず摘要としてメモ末尾に保持。
  伐採・軽剪定の自動判定からも除外される

### 判定・集計

- **伐採判定の修正**: 「伐採含む」「伐採済(み)」「〜で伐採」「〜から伐採」は
  剪定行の注記として扱い、伐採集計に流れないようにした。
  伐採行は「樹種　c〇〇　伐採」の単独表記
- **支障枝・枯れ枝 → 軽剪定の自動判定**: メモに「支障」「枯れ枝」「枯枝」を含む剪定行は
  軽剪定として集計（優先順位: 伐採 ＞ 軽剪定キーワード ＞ 強剪定）。
  ランク境界も軽剪定用（C<15 / C=15-30 あり）に切り替わる
- **樹高ランクの是正**: h<150 → `H=1.5m未満` / 150≦h<250 → `H=1.5-2.5m` /
  h≧250 → `H=2.5m以上`（`h150` ちょうど・`h150以上` は H=1.5-2.5m 側）
- **フジ棚の専用集計**: 樹種名に「フジ」を含む w/d 面積行は
  「低木剪定(強剪定) / フジ棚(冬季) / 規格なし / m2」として集計・転記

### 樹種マスタ

- **組み込みマスタ追記**: ツバキ(常緑E)・ツゲ(低木L)・ハゼノキ(落葉D)・ヒメユズリハ(常緑E)・
  アカメガシワ(落葉D)・キョウチクトウ(常緑E)・ナワシログミ(常緑E)・ヒマラヤスギ(針葉C)
- **部分一致の是正**: 「樹種名がマスタキーを含む」方向のみに限定
  （ヒマラヤスギ→スギは可、ツバキ→ナツツバキの誤爆(落葉D誤判定)を解消）
- **getSpeciesType 二重定義バグ修正**: function 二重宣言の巻き上げにより
  ユーザーマスタ登録種が集計に反映されなかった既存バグを解消
  （組み込み版を `getSpeciesTypeBuiltin` に改名し、ユーザーマスタ優先版から呼ぶ構成に）

### メモの書き方ルール（v3.2 以降の運用）

1. 日付・班名・`---` はそのまま書いてよい（自動スキップ）
2. 公園は `# 公園名`。`---` を跨いだら次の `#` まで品目行は無視される
3. 低木寄植えは `樹種　hランク　wXXX dXXX`（cm）だけで面積が自動計算される
4. 同寸複数は `×N個` または `×N箇所`
5. ランクは以上/以下付き（`h150以下`）、実測は裸 h（`h120`）で併記可 → 実測はメモに保持
6. 伐採対象行は `伐採` を単独で書く。剪定行の注記は「伐採含む」「〜で伐採」「〜から伐採」、
   または `//` 以降に書く（例: `クスノキ　c40 伐採 //鉄棒裏` は伐採、
   `イチョウ　c30-60 //株立の細い方は根本から伐採` は剪定）
7. 軽剪定にしたい行は `軽剪定`・`支障枝`・`枯れ枝` のいずれかを **`//` より前に**書く
   （`//` 以降は判定対象外。何も書かなければ強剪定）
8. 剪定は1行1種。複合種の行は伐採行のみ可（伐採は種別不問のため）

## v3.1 追補（2026-07-15）

- **一括削除機能**: 「選択して一括削除」モード（カードをタップして複数選択→まとめて削除、
  表示中の全選択/全解除）と、公園タブ選択中の「この公園をまるごと削除」を追加。
  複数件の削除は単一の IndexedDB トランザクションで実行し、削除前に件数付き確認を表示
